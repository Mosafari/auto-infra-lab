apiVersion: 1

providers:
  - name: 'default-dashboards'
    folder: 'Default'
    type: file
    options:
      path: /var/lib/grafana/dashboards

    jsonData:
      templating:
        list:
          - name: datasource
            type: constant
            value: Prometheus
